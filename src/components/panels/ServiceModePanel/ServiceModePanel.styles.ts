export const serviceModeBannerStyle = (theme: TTheme) => ({
  color: theme.grey1Color,
  backgroundColor: theme.brandDarkColor,
  display: "flex",
  justifyContent: "center",
});

export const wrapperContentStyle = {
  maxWidth: "730px",
};

export const iconStyle = {
  fontSize: "14px",
  padding: "2px 1px",
};
